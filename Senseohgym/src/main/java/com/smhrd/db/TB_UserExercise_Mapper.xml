<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.smhrd.model.TB_UserExerciseDAO">
  	<select id="select" parameterType="TB_UserExerciseDTO" resultType="TB_UserExerciseDTO">

		SELECT RS_USETIME, REV_MACHINE 
		  FROM TB_USER_EXERCISE
		 WHERE MB_CARD = #{mb_card} 	
		
      <!--
        SELECT * FROM TB_USER_EXERCISE
		WHERE MB_NAME = #{mb_name} 
		AND MB_CARD = #{mb_card}
		-->	
	</select>
  
  
  
  
  </mapper>